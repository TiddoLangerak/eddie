#!/usr/bin/env bash
set -euo pipefail

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Run the serve.ts file with node
node --watch --experimental-strip-types --conditions=development "$SCRIPT_DIR/serve.ts" "$@"
