Editor for Beancount files.

- **Account autocomplete**: In account and source-account fields, typing shows hierarchical suggestions (segment completions like `Equity:` first, then full accounts), with fuzzy matching, using all accounts from the workspace.
